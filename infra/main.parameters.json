{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "foundryName": {
      "value": "${FOUNDRY_NAME=}"
    },
    "bingName": {
      "value": "${BING_NAME=}"
    },
    "modelDeploymentName": {
      "value": "${MODEL_DEPLOYMENT_NAME=}"
    },
    "modelName": {
      "value": "${MODEL_NAME=}"
    },
    "modelVersion": {
      "value": "${MODEL_VERSION=}"
    },
    "modelCapacity": {
      "value": "${MODEL_CAPACITY=0}"
    },
    "imageModelDeploymentName": {
      "value": "${IMAGE_MODEL_DEPLOYMENT_NAME=}"
    },
    "imageModelName": {
      "value": "${IMAGE_MODEL_NAME=}"
    },
    "imageModelVersion": {
      "value": "${IMAGE_MODEL_VERSION=}"
    },
    "imageModelCapacity": {
      "value": "${IMAGE_MODEL_CAPACITY=0}"
    },
    "deployImageModel": {
      "value": "${DEPLOY_IMAGE_MODEL=false}"
    },
    "principalId": {
      "value": "${AZURE_PRINCIPAL_ID}"
    }
  }
}